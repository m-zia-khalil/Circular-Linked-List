# Circular-Linked-List
This project implements a circular linked list in C++. It creates nodes dynamically, links them sequentially, and connects the last node back to the head to form a loop. The program traverses and displays elements using a do-while loop, demonstrating pointers and dynamic memory usage.
